# Impact Map Framework Dependencies
# 현재 구현은 Python 표준 라이브러리만 사용 (json, ast, re, os, collections, argparse)
# 아래는 향후 확장 시 사용할 수 있는 패키지 (현재 미사용)

# --- 향후 확장용 (Optional) ---
# networkx==3.1          # 그래프 분석 고도화 (PageRank, 중심성 분석 등)
# PyGithub==2.1.1        # GitHub API 연동 (PR 자동 코멘트, Webhook)
# openai>=1.0.0          # LLM 기반 코드 변경 요약 및 위험도 판단
